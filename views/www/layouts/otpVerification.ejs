<!DOCTYPE html>
<html lang="en">
<head>
   
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Otp Verification</title>
    <style>
        body {
            margin: 0;
            font-family: 'Helvetica', sans-serif;
            background-color: #fdfeff; /* Light gray background */
        }

        .login-page {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(to right, #edebef, #042f79); /* Gradient background */
            color: white;
        }

        .login-form {
            background: #fff; /* White background for form */
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 25px rgba(0, 0, 0, 0.2);
            width: 350px;
        }

        .login-form h2 {
            margin-bottom: 20px;
            text-align: center;
            color: #333; /* Darker text for better readability */
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            margin-bottom: 5px;
            color: #555; /* Dark gray label */
        }

        .form-group input {
            width: 100%;
            padding: 12px;
            border: 1px solid #ccc;
            border-radius: 5px;
            transition: border-color 0.3s;
        }

        .form-group input:focus {
            border-color: #6a11cb; /* Purple focus color */
            outline: none;
        }

        button {
            width: 100%;
            padding: 12px;
            background-color: #2575fc; /* Blue button color */
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #1e60d2; /* Darker blue on hover */
        }

        .login-footer {
            margin-top: 15px;
            text-align: center;
            font-size: 14px;
            color: #555;
        }

        .login-footer a {
            color: #2575fc;
            text-decoration: none;
        }

        .login-footer a:hover {
            text-decoration: underline; /* Underline on hover */
        }
    </style>
</head>
<body>
  
    <div class="login-page">
        <div class="login-form">
            <h2>Otp Verification</h2>
            <form action="/otp/verify" method="POST">
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" required>
                </div>
                <div class="form-group">
                    <label for="otp">Otp:</label>
                    <input type="text" id="otp" name="otp" required>
                </div>
                <button type="submit">Submit</button>
            </form>
        </div>
    </div>
</body>
</html>
